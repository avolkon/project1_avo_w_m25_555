# def parse(cmd):

#     '''
#     центральный обработчик ввода пользователя.
#     Она принимает строку команды, разбирает её и направляет
#     на выполнение соответствующей игровой функции
#     (перемещение, взятие предмета, просмотр инвентаря и т. д.).
#     '''

#     words = cmd.lower().strip().split() # преобразует входную строку cmd в список слов
#     if not words: return # проверяет, не является ли список words пустым
    
#     action = words[0] # берёт первое слово из списка words и сохраняет в переменную action
#     if action == 'идти' and len(words) > 1: # проверяет наличие направления во вводе с командой "идти"
#         move(words[1]) # передаёт второе слово из ввода (направление) в функцию move()
#     elif action == 'взять' and len(words) > 1: # аналогично предыдущему блоку с командой "идти"
#         take(words[1]) # передаёт название предмета в функцию take()
#     elif action == 'артефакты': # обработка команды "артефакты"
#         artifacts_list() # вызывает функцию показа инвентаря
#     elif action in ['помощь', 'help', 'f1']: # реагирует на синонимы команды справки
#         help() # запускает вывод списка команд (функцию help())
#     elif action == 'осмотреть': # обрабатывает команду 'осмотреть'
#         show_room() # перерисовывает текущую комнату (вызывает show_room())
#     else:
#         solve(cmd) # срабатывает, если команда не совпала ни с одним из предыдущих вариантов.

