# def parse(cmd):

#     '''
#     центральный обработчик ввода пользователя.
#     Она принимает строку команды, разбирает её и направляет
#     на выполнение соответствующей игровой функции
#     (перемещение, взятие предмета, просмотр инвентаря и т. д.).
#     '''

#     words = cmd.lower().strip().split() # преобразует входную строку cmd в список слов
#     if not words: return # проверяет, не является ли список words пустым
    
#     action = words[0] # берёт первое слово из списка words и сохраняет в переменную action
#     if action == 'идти' and len(words) > 1: # проверяет наличие направления во вводе с командой "идти"
#         move(words[1]) # передаёт второе слово из ввода (направление) в функцию move()
#     elif action == 'взять' and len(words) > 1: # аналогично предыдущему блоку с командой "идти"
#         take(words[1]) # передаёт название предмета в функцию take()
#     elif action == 'артефакты': # обработка команды "артефакты"
#         artifacts_list() # вызывает функцию показа инвентаря
#     elif action in ['помощь', 'help', 'f1']: # реагирует на синонимы команды справки
#         help() # запускает вывод списка команд (функцию help())
#     elif action == 'осмотреть': # обрабатывает команду 'осмотреть'
#         show_room() # перерисовывает текущую комнату (вызывает show_room())
#     else:
#         solve(cmd) # срабатывает, если команда не совпала ни с одним из предыдущих вариантов.


def move(direction):
    '''Суть: перемещает игрока в указанную комнату.
    Логика:
    1. Проверяет, существует ли выход в направлении direction.
    2. Если да — обновляет current_room, увеличивает счётчик шагов,
    показывает новую комнату и проверяет победу.
    3. Если нет — выводит ошибку с подсказкой доступных направлений.
    '''

    exits = ROOMS[game_state['current_room']]['exits'] #получает словарь выходов (направлений) для текущей комнаты.
    if direction in exits: # проверяет, существует ли указанное направление в списке доступных выходов
        game_state['current_room'] = exits[direction] # обновляет текущую комнату игрока
        game_state['steps'] += 1 # увеличивает счётчик пройденных шагов на 1 и отслеживает, сколько ходов сделал игрок (для статистики в конце игры).
        show_room() # выводит описание новой комнаты, доступные выходы, предметы и другую инфо
        check_win() # проверяет, достиг ли игрок финальной комнаты и выполнил ли все условия
    else:
        print(f"❌ Нет '{direction}'. Доступно: {', '.join(exits.keys())}")
        # срабатывает, если игрок попытался пойти в недоступном направлении
        # (например, ввёл идти запад, а выхода на запад нет)

def take(item):

    '''Суть: позволяет взять предмет из комнаты.
    Логика:
    1. Проверяет наличие предмета в комнате (room_items).
    2. Убеждается, что артефакт ещё не собран (нет в artifacts).
    3. Добавляет предмет в artifacts, удаляет из комнаты, выводит сообщение.
    4. Если предмет уже есть — сообщает об этом.
    5. Если предмета нет — выводит ошибку.
    '''

    room_items = ROOMS[game_state['current_room']]['items'] # получает список предметов, доступных в текущей комнате
    if item in room_items: # проверяет, есть ли запрашиваемый fhntafrn в списке предметов комнаты.
        if item not in game_state['items']: # проверяет, нет ли этого предмета уже в инвентаре игрока
            game_state['items'].append(item) # добавляет предмет в набор артефактов игрока
            room_items.remove(item) # удаляет предмет из комнаты (чтобы его нельзя было взять повторно).
            print(f"✅ Взят: {item}")
        else:
            print("❌ Уже есть!")
    else:
        print("❌ Нет предмета")

def items_list():

    '''Суть: показывает список собранных артефактов.
    Особенности:
    1. Если артефактов нет — выводит «пуст».
    2. Отображает количество артефактов в скобках.
    '''
    items = game_state['items'] # создаёт ссылку на список артефактов в инвентаре игрока.
    print(f"💎 ({len(items)}): {', '.join(items) or 'пуст'}")

# Функция use_item прямая реализация

def use_item(game_state, item_name):
    '''
    Использует предмет из набора артефактов.
    '''
    items = game_state.get('items', [])
    
    if item_name not in items:
        print("❌ У вас нет такого предмета.")
        return False
    
    if item_name == 'torch':
        print("🔥 Можешь освещать свой путь! Но расходуй заряд экономно.")
    
    elif item_name == 'sword':
        print("⚔️ Уверенность!")
    
    elif item_name == 'bronze_box':
        if 'rusty_key' not in items:
            game_state['items'].append('rusty_key')
            print("✅ Теперь у тебя есть rusty_key!")
        else:
            print("✅ Шкатулка пуста.")
    
    elif item_name == 'golden_chest':
        print("🏆 ПОБЕДА!")
    
    elif item_name == 'candle':
        print("🕯️ Источник света для прочтения книги!")
    
    elif item_name == 'silver_cross':
        print("✝️ Защита от опасных артефактов и обитателей лабиринта!")

    elif item_name == 'ancient_book':
        print("📖 Теперь тебе доступны древние знания!")

    elif item_name == 'rotten_wood':
        print("🌲 Гнилые дрова — бесполезны.")

    elif item_name == 'glowing_mushroom':
        print("🍄 Гриб светится, пока растёт, лучше его не брать, \nпоможет сэкономить энергию твоих источников света.")

    elif item_name == 'water_flask':
        print("💧 Живая вода восполнила силы и улучшила смекалку!")

    elif item_name == 'small_boat':
        print("🚤 Лодка поможет добраться до центра источника и набрать живой воды.")

    else:
        print(f"❓ Не знаете '{item_name}'.")
    
    return True

# Добавьте в player_actions.py:
