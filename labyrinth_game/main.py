# def main():
#     """Основная функция программы."""
#     print("Первая попытка запустить проект!")

# # Проверка: запущен ли этот модуль как программа (а не импортирован)
# if __name__ == "__main__":
#     main()

# Импорт модулей игры
from labyrinth_game.constants import ROOMS, START_ROOM, WIN_ROOM, COMMANDS, MESSAGES
from labyrinth_game.player_actions import move_player, take_item, use_item, show_items, get_input, back
from labyrinth_game.utils import describe_room, solve_puzzle

# Определение состояния игрока
game_state = {
    'items': [],  # артефакты игрока ['torch', 'rusty_key' итд.]
    'rooms': ROOMS,   
    'current_room': START_ROOM, # 'entrance'
    'previous_room': None, # Стартовое значение для предыдущей комнаты
    'game_over': False, # Конец игры
    'steps': 0,  # Счетчик ходов
    'puzzles_solved': set(), # Подсчёт решенных загадок
    'coins' : 0,
}

def show_help(COMMANDS):
    """
    Выводит справочную информацию по командам с красивым выравниванием.
    Args:
        COMMANDS: список команд из constants.py
    """
    print("\n📋 ДОСТУПНЫЕ КОМАНДЫ:")
    for command in COMMANDS:
        # ✅ 16 пробелов + f-строка для выравнивания
        print(f"  {command:<16}")

# def show_help(): # не принимает аргументов;
#     #предназначена для вывода справочной информации по командам игры

#     '''Показывает список команд'''
#     print("\n📋 ДОСТУПНЫЕ КОМАНДЫ:")
#     print("  go | идти <направление>      - идти (north, south, east, west)")
#     print("  back | назад                 - вернуться назад")
#     print("  take | взять <item>          - взять предмет")
#     print("  use | применить <item>       - использовать предмет")
#     print("  look | осмотреть             - осмотреть комнату")
#     print("  items | артефакты            - показать артефакты")
#     print("  solve | решить               - решить загадку")
#     print("  help | помощь                - помощь")
#     print("  exit | выход                 - выход из игры")


def process_command(game_state, command):
    '''
    Обрабатывает команды с match/case.
    '''
    parts = command.lower().strip().split(maxsplit=1)
    action = parts[0] if parts else '' # Извлекает первое слово (действие) из parts
    arg = parts[1] if len(parts) > 1 else '' # Извлекает второй элемент (аргумент) из parts, если он есть
    
    match action:
        case 'go' | 'идти': # Срабатывает, если action равен 'go' или 'идти'
            if arg: move_player(game_state, arg) #Если есть аргумент (arg не пуст),
            # вызывает move_player с текущим состоянием и направлением
            # Например: go north → move_player(game_state, 'north')
            else: print("❌ Куда идти?")

        case 'back' | 'назад' | 'вернуться':
            back(game_state)
        
        case 'take' | 'взять':
            if arg: take_item(game_state, arg) # Если есть аргумент, вызывает take_item (подбор предмета)
            else: print("❌ Что взять?")
        
        case 'use' | 'применить':
            if arg: use_item(game_state, arg) # Вызывает use_item с аргументом (названием предмета)
            else: print("❌ Что использовать?")
        
        case 'look' | 'осмотреть':
            describe_room(game_state) # Вызывает функцию, выводящую описание комнаты
        
        case 'items' | 'артефакты':
            show_items(game_state) # Вызывает функцию, показывающую список артефактов игрока
        
        case 'help' | 'помощь':
            show_help() # Выводит подсказку по командам
        
        case 'quit' | 'exit' | 'выход':
            game_state['game_over'] = True # Устанавливает флаг окончания игры в состоянии True
            print("👋 До свидания!")
        
        case 'solve' | 'решить':
            solve_puzzle(game_state)  # Вызов функции загадок
            # Вызывает функцию solve с полной командой
            # (предполагается, что это загадка или нестандартная команда)
        case _:
            print("🛑 Неизвестная команда. Введи 'help' или 'помощь' для справки.")
    
def main():

    '''
    Функция main() — точка входа в игру.
    Она запускает игровой процесс:
    выводит приветствие, показывает стартовую комнату
    и организует бесконечный цикл
    обработки команд игрока до завершения игры.
    '''

    print("🔥 Добро пожаловать в Лабиринт сокровищ! 🔥")
    print("🎯 Тебя ждёт победа при достижении treasure_room и открытии сундука с сокровищами (treasure_chest)")
    describe_room(game_state)
    
    while not game_state['game_over']:
        # запускает цикл, который работает, пока игра не завершена
        # game_state['game_over'] — флаг в состоянии игры:
        # False — игра продолжается → цикл работает
        # True — игра завершена → цикл прерывается
        try: 
            # начинает блок обработки исключений, защищает программу
            # от аварийного завершения при неожиданных прерываниях
            # (например, если игрок нажмёт Ctrl+C)

            cmd = get_input("\nВведи команду: действие + направление\n(для справки help или помощь)\n> ")
            # Выводит приглашение, Передаёт введённую строку в функцию process_command()
            # Эффект: игрок вводит команду (например, go north), она обрабатывается
            process_command(game_state, cmd)
            # Функция разбирает команду и выполняет соответствующее действие
            # (перемещение, подбор предмета и т. д.)

        except (KeyboardInterrupt, EOFError): # перехватывает исключения 2 типов
            print("\n👋 Выход.")
            break

if __name__ == "__main__":
    # проверяет, запускается ли скрипт напрямую (а не импортируется как модуль)
    # Если файл запущен самостоятельно, __name__ будет равен "__main__"
    # Если импортирован в другой скрипт, условие не выполнится (чтобы не запускать игру случайно)
    main()
    # вызывает функцию main(), которая:
    # выводит приветствие;
    # показывает стартовую комнату;
    # запускает игровой цикл.
    # Cрабатывает только если скрипт запущен напрямую
