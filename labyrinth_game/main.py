# def main():
#     """Основная функция программы."""
#     print("Первая попытка запустить проект!")

# # Проверка: запущен ли этот модуль как программа (а не импортирован)
# if __name__ == "__main__":
#     main()

# Импорт модулей игры
from labyrinth_game import ROOMS, START_ROOM, WIN_ROOM, COMMANDS, MESSAGES
from labyrinth_game import move_player, take_item, use_item, look_items, get_input
from labyrinth_game import describe_room

def main():

    '''
    Функция main() — точка входа в игру.
    Она запускает игровой процесс:
    выводит приветствие, показывает стартовую комнату
    и организует бесконечный цикл
    обработки команд игрока до завершения игры.
    '''

    print("🔥 Добро пожаловать в Лабиринт сокровищ! 🔥")
    print("🎯 Тебя ждёт победа при достижении treasure_room и получении артефакта golden_chest")
    show_room()
    
    while not game_state['game_over']:
        # запускает цикл, который работает, пока игра не завершена
        # game_state['game_over'] — флаг в состоянии игры:
        # False — игра продолжается → цикл работает
        # True — игра завершена → цикл прерывается
        try: 
            # начинает блок обработки исключений, защищает программу
            # от аварийного завершения при неожиданных прерываниях
            # (например, если игрок нажмёт Ctrl+C)

            cmd = get_input("\nВведите команду: действие + направление\n(для справки help или помощь)\n> ")
            # Выводит приглашение, Передаёт введённую строку в функцию parse()
            # Эффект: игрок вводит команду (например, go north), она обрабатываетс
            process_command(game_state, cmd)
            # Функция разбирает команду и выполняет соответствующее действие
            # (перемещение, подбор предмета и т. д.)

        except (KeyboardInterrupt, EOFError): # перехватывает исключения 2 типов
            print("\n👋 Выход.")
            break

if __name__ == "__main__":
    # проверяет, запускается ли скрипт напрямую
    # (а не импортируется как модуль)
    # Если файл запущен самостоятельно, __name__ будет равен "__main__"
    # Если импортирован в другой скрипт,
    # условие не выполнится (чтобы не запускать игру случайно)
    main()
    # вызывает функцию main(), которая:
    # выводит приветствие;
    # показывает стартовую комнату;
    # запускает описанный выше игровой цикл.
    # Cрабатывает только если скрипт запущен напрямую

# Определение состояния игрока
game_state = {
    'items': [],  # артефакты игрока ['torch', 'rusty_key' итд.]        
    'current_room': START_ROOM, # 'entrance'
    'game_over': False, # Конец игры
    'steps': 0,  # Счетчик ходов
    'puzzles_solved': 0  # 0-6 загадок
}



solved_puzzles = set() # {'hall', 'library'}

def show_room():
    '''Суть: выводит информацию о текущей комнате.
    Что делает:
    1. Получает данные комнаты из ROOMS по ключу current_room.
    2. Печатает название и описание локации.
    3. Если есть нерешённая загадка — выводит её текст.
    4. Отображает доступные выходы (exits) и предметы (items).
    5. Показывает статистику: количество шагов и собранных артефактов.
    '''

    room = ROOMS[game_state['current_room']]
    print(f"\n{'═' * 50}")
    print(f"📍 {game_state['current_room'].title()}")
    print(room['description'])
    
    if room.get('puzzle') and game_state['current_room'] not in solved_puzzles:
        print(f"\n🔒 ЗАГАДКА:\n{room['puzzle'][0]}")
    
    print(f"\n→ Выходы: {', '.join(room['exits'].keys())}")
    if room['items']:
        print(f"💎 Найдено: {', '.join(room['items'])}")
    
    print(f"★ Шагов: {game_state['steps']} | 💎 Артефактов: {len(game_state['items'])}")


def solve(answer):

    '''
    Суть: проверяет ответ на загадку текущей комнаты.
    Логика:
    1. Убеждается, что загадка существует и не решена.
    2. Сравнивает введённый ответ с правильным (без учёта регистра и пробелов).
    3. При верном ответе:
        Добавляет комнату в solved_puzzles.
        Увеличивает счётчик puzzles_solved.
        Выводит сообщение о победе.
    4. При ошибке — предлагает повторить попытку.
    '''

    room = ROOMS[game_state['current_room']] # получает данные текущей комнаты из глобального словаря локаций.
    if room.get('puzzle') and game_state['current_room'] not in solved_puzzles: # проверяет два условия^
        # В текущей комнате есть загадка
        # Эта комната ещё не отмечена как «решённая» (current_room отсутствует в множестве solved_puzzles)
        if answer.strip().lower() == room['puzzle'][1].strip().lower(): # сравнивает ответ игрока с правильным решением загадки
            solved_puzzles.add(game_state['current_room']) # добавляет текущую комнату в множество решённых
            game_state['puzzles_solved'] += 1 # увеличивает счётчик решённых загадок на 1
            print("🎉 Загадка решена!")
            return True
        print("❌ Неверно! Попробуйте ещё.")
        return False

def check_win():
    
    '''Суть: проверяет условия победы.
    Условия:
    1. Игрок находится в WIN_ROOM.
    2. В инвентаре есть 'golden_chest'.
    Результат:
    1. Вывод сообщения о победе.
    2. Отображение статистики (шаги, решённые загадки, артефакты).
    3. Установка флага game_over = True для завершения игры.
    '''

    if (game_state['current_room'] == WIN_ROOM and
        'golden_chest' in game_state['items']):
        # проверяет два обязательных условия победы одновременно
        # игрок находится в финальной комнате
        # в инвентаре игрока (artifacts) есть предмет 'golden_chest'
        print(f"\n{'🎉' * 25}")
        print("🏆 ПОБЕДА! Сокровища ваши!")
        print(f"📈 {game_state['steps']} шагов | {game_state['puzzles_solved']}/6")
        print(f"Артефакты: {game_state['items']}")
        game_state['game_over'] = True

def f1_help(): # не принимает аргументов;
    #предназначена для вывода справочной информации по командам игры

    '''Суть: выводит список доступных команд и примеры их использования.
    Формат:
    Краткий перечень команд.
    Примеры ввода (например, идти север).
    '''

    print("\n📋 КОМАНДЫ:")
    print("идти | go/взять | take/артефакты | items/помощь | help/осмотреть | look")
    print("💡 go north | take torch")

def describe_room(game_state):
    '''look/осмотреть — аналог show_room без загадки.'''
    show_room()  # Используем существующую

def process_command(game_state, command):
    '''
    Обрабатывает команды с match/case.
    '''
    parts = command.lower().strip().split(maxsplit=1)
    action = parts[0] if parts else '' # Извлекает первое слово (действие) из parts
    arg = parts[1] if len(parts) > 1 else '' # Извлекает второй элемент (аргумент) из parts, если он есть
    
    match action:
        case 'go' | 'идти': # Срабатывает, если action равен 'go' или 'идти'
            if arg: move_player(game_state, arg) #Если есть аргумент (arg не пуст),
            # вызывает move_player с текущим состоянием и направлением
            # Например: go north → move_player(game_state, 'north')
            else: print("❌ Куда идти?")
        
        case 'take' | 'взять':
            if arg: take_item(game_state, arg) # Если есть аргумент, вызывает take_item (подбор предмета)
            else: print("❌ Что взять?")
        
        case 'use' | 'применить':
            if arg: use_item(game_state, arg) # Вызывает use_item с аргументом (названием предмета)
            else: print("❌ Что использовать?")
        
        case 'look' | 'осмотреть':
            describe_room(game_state) # Вызывает функцию, выводящую описание комнаты
        
        case 'items' | 'артефакты':
            look_items(game_state) # Вызывает функцию, показывающую список артефактов игрока
        
        case 'help' | 'помощь':
            f1_help() # Выводит подсказку по командам
        
        case 'quit' | 'exit' | 'выход':
            game_state['game_over'] = True # Устанавливает флаг окончания игры в состоянии True
            print("👋 До свидания!")
        
        case _:
            solve(command)  # Загадка по умолчанию (как в parse)
            # Вызывает функцию solve с полной командой
            # (предполагается, что это загадка или нестандартная команда)
    
    # Проверка победы после команды
    check_win()

